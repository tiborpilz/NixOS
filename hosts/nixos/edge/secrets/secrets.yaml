# Placeholder: encrypt this file with sops after deploying the edge node.
#
# Steps:
#   1. Deploy the edge node (nixos-rebuild switch --flake .#edge --target-host root@<EDGE_IP>)
#   2. Get the generated age public key:
#      ssh root@<EDGE_IP> cat /var/lib/sops-nix/key.txt | age-keygen -y
#   3. Add the key to .sops.yaml (see root-level .sops.yaml for reference)
#   4. Populate and encrypt:
#      sops --encrypt --age <EDGE_AGE_PUBKEY> hosts/nixos/edge/secrets/secrets.yaml
#
# Required keys:
#   netmakerEnv: |
#     MASTER_KEY=<random-string>
#     MQ_ADMIN_PASSWORD=<random-string>
netmakerEnv: placeholder
