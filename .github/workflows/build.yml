name: 'Build Emacs'
on:
  push:
    branches:
      - main
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build-emacs-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    name: build-emacs-linux
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
    - uses: cachix/install-nix-action@08dcb3a5e62fa31e2da3d490afc4176ef55ecd72 # v30
      with:
        nix_path: nixpkgs=channel:nixos-unstable
        extra_nix_config: |
          download-buffer-size = 524288000
          sandbox = false
    - uses: cachix/cachix-action@ad2ddac53f961de1989924296a1f236fcfbaa4fc # v15
      with:
        name: tiborpilz
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    - run: |
        for attr in emacs doom-emacs doom-emacs-standalone; do
          nix run github:Mic92/nix-fast-build -- \
            --skip-cached \
            --no-nom \
            --flake ".#checks.x86_64-linux.$attr"
        done

  # build-linux:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     id-token: write
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       check:
  #         # - home-tibor
  #         # - edge
  #         # - klaus
  #         # - thinkyMcThinkpad
  #         - doom-emacs
  #         # - testTandoorUpgrade
  #         # - testPaperlessUpgrade
  #   name: build-linux (${{ matrix.check }})
  #   steps:
  #   - name: Free Disk Space (Ubuntu)
  #     uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
  #   - uses: cachix/install-nix-action@08dcb3a5e62fa31e2da3d490afc4176ef55ecd72 # v30
  #       nix_path: nixpkgs=channel:nixos-unstable
  #       extra_nix_config: |
  #         download-buffer-size = 524288000
  #         sandbox = false
  #   - uses: cachix/cachix-action@ad2ddac53f961de1989924296a1f236fcfbaa4fc # v15
  #       name: tiborpilz
  #       authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
  #   - run: |
  #       nix run github:Mic92/nix-fast-build -- \
  #         --skip-cached \
  #         --no-nom \
  #        --flake '.#checks.x86_64-linux.${{ matrix.check }}'

  build-emacs-mac:
    runs-on: macos-14
    permissions:
      contents: read
      id-token: write
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
    - uses: cachix/install-nix-action@08dcb3a5e62fa31e2da3d490afc4176ef55ecd72 # v30
      with:
        nix_path: nixpkgs=channel:nixos-unstable
        extra_nix_config: |
          download-buffer-size = 524288000
          sandbox = false
    - uses: cachix/cachix-action@ad2ddac53f961de1989924296a1f236fcfbaa4fc # v15
      with:
        name: tiborpilz
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    - run: |
        for attr in emacs doom-emacs doom-emacs-standalone; do
          nix run github:Mic92/nix-fast-build -- \
            --skip-cached \
            --no-nom \
            --flake ".#checks.aarch64-darwin.$attr"
        done
