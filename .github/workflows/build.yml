name: 'Build Emacs'
on:
  push:
    branches:
      - main
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build-emacs-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    name: build-emacs-linux
    steps:
    - uses: actions/checkout@v4
    - uses: cachix/install-nix-action@v30
      with:
        nix_path: nixpkgs=channel:nixos-unstable
        extra_nix_config: |
          download-buffer-size = 524288000
          sandbox = false
    - uses: cachix/cachix-action@v15
      with:
        name: tiborpilz
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    - run: |
        nix run github:Mic92/nix-fast-build -- \
          --skip-cached \
          --no-nom \
          --flake '.#doomemacs'
  
  # build-linux:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     id-token: write
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       check:
  #         # - home-tibor
  #         # - edge
  #         # - klaus
  #         # - thinkyMcThinkpad
  #         - doom-emacs
  #         # - testTandoorUpgrade
  #         # - testPaperlessUpgrade
  #   name: build-linux (${{ matrix.check }})
  #   steps:
  #   - name: Free Disk Space (Ubuntu)
  #     uses: jlumbroso/free-disk-space@main
  #   - uses: actions/checkout@v4
  #   - uses: cachix/install-nix-action@v30
  #     with:
  #       nix_path: nixpkgs=channel:nixos-unstable
  #       extra_nix_config: |
  #         download-buffer-size = 524288000
  #         sandbox = false
  #   - uses: cachix/cachix-action@v15
  #     with:
  #       name: tiborpilz
  #       authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
  #   - run: |
  #       nix run github:Mic92/nix-fast-build -- \
  #         --skip-cached \
  #         --no-nom \
  #        --flake '.#checks.x86_64-linux.${{ matrix.check }}'

  build-enacs-mac:
    runs-on: macos-14
    steps:
    - uses: actions/checkout@v4
    - uses: cachix/install-nix-action@v30
      with:
        nix_path: nixpkgs=channel:nixos-unstable
        extra_nix_config: |
          download-buffer-size = 524288000
          sandbox = false
    - uses: cachix/cachix-action@v15
      with:
        name: tiborpilz
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    - run: |
        nix run github:Mic92/nix-fast-build -- \
          --skip-cached \
          --no-nom \
          --flake '.#doomemacs'
