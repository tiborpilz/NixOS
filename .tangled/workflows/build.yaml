when:
  - event: ["pull_request", push", "manual"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - nix

steps:
  - name: "Build flakeRepl"
    command: "nix --extra-experimental-features 'nix-command flakes' build .#flakeRepl"
